#!/usr/bin/env bash
# Mock for xdg-open
# Logs invocation to MOCK_LOG, returns configurable output
MOCK_NAME="xdg-open"
echo "$MOCK_NAME $*" >> "${MOCK_LOG:-/dev/null}"
if [[ -n "${MENU_MOCK_OUTPUT:-}" && -f "$MENU_MOCK_OUTPUT" ]]; then
  cat "$MENU_MOCK_OUTPUT"
fi
exit 0
